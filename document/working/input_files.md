# 入力ファイルの仕様

## 概要
アプリケーション層再構築に伴い、入力ファイル仕様は再定義中。

## 現在の扱い

- 旧仕様の `data/run_config/*.toml` を現行仕様としては固定しない
- 新しいユースケース定義・入力スキーマは `working/` 側で確定後にここへ集約する
- 実装再開までは、既存ファイルは「検討用サンプル」として扱う

## 旧仕様の参照先

- 旧CLIと run_config 運用の詳細はアーカイブ資料（`document/class_design/`）を参照
